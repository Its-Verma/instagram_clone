<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Add New Post</title>
    <link rel="stylesheet" href="/css/add-post-styles.css" />
  </head>
  <body>
    <div class="container">
      <h2>Edit Your Post</h2>
      <form action="/instagram/<%=post[0].id %>?_method=patch" method="post">
        <!-- Add Username -->
        <div class="form-group">
          <label for="username">Username:</label>
          <input
            type="text"
            id="username"
            name="username"
            value="<%= post[0].username %>"
            placeholder="Enter your username..."
            required
            disabled
          />
        </div>
        <!-- Upload Post Image -->

        <div class="form-group">
          <label for="post_pic">Uploaded Post Image:</label>
          <input
            type="text"
            id="post-image"
            name="post_pic"
            value="<%= post[0].post_pic %>"
            accept="image/*"
            required
            disabled
          />
        </div>
        <!-- Upload Profile Image -->

        <div class="form-group">
          <label for="profile_pic">Uploaded Profile Picture:</label>
          <input
            type="text"
            id="profile-image"
            name="profile_pic"
            value="<%= post[0].profile_pic %>"
            required
            disabled
          />
        </div>

        <!-- Post Caption -->
        <div class="form-group">
          <label for="content">Caption:</label>
          <textarea
            id="post-caption"
            name="content"
            rows="4"
            placeholder="Write your caption here..."
            required
          >
<%= post[0].content %></textarea
          >
        </div>

        <!-- Submit Button -->
        <div class="form-group">
          <button type="submit" class="submit-button">Post</button>
        </div>
      </form>
    </div>
  </body>
</html>
